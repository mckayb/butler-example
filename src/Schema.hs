module Schema where

import Data.Text (Text)
import User.User

-- data Relationship = HasOne | BelongsTo | HasMany | BelongsToMany

-- data Schema a = Schema { schemaName :: Text, schemaValue :: a, schemaRelationships :: [r] }